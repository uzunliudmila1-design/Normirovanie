from docx import Document
from docx.shared import Pt, RGBColor, Inches
from docx.enum.text import WD_ALIGN_PARAGRAPH

doc = Document()

# Поля страницы
for section in doc.sections:
    section.top_margin = Inches(1)
    section.bottom_margin = Inches(1)
    section.left_margin = Inches(1.2)
    section.right_margin = Inches(1.2)

def heading1(text):
    p = doc.add_heading(text, level=1)
    p.runs[0].font.color.rgb = RGBColor(0x1F, 0x49, 0x7D)
    p.runs[0].font.size = Pt(16)

def heading2(text):
    p = doc.add_heading(text, level=2)
    p.runs[0].font.color.rgb = RGBColor(0x2E, 0x74, 0xB5)
    p.runs[0].font.size = Pt(13)

def para(text, bold=False, italic=False):
    p = doc.add_paragraph()
    run = p.add_run(text)
    run.bold = bold
    run.italic = italic
    run.font.size = Pt(11)
    p.paragraph_format.space_after = Pt(4)
    return p

def bullet(text, bold_prefix=None):
    p = doc.add_paragraph(style='List Bullet')
    if bold_prefix:
        run1 = p.add_run(bold_prefix)
        run1.bold = True
        run1.font.size = Pt(11)
        run2 = p.add_run(text)
        run2.font.size = Pt(11)
    else:
        run = p.add_run(text)
        run.font.size = Pt(11)
    p.paragraph_format.space_after = Pt(3)

def separator():
    doc.add_paragraph()

# ─── ЗАГОЛОВОК ───────────────────────────────────────────────────
title = doc.add_heading('Система автоматического нормирования операций', 0)
title.alignment = WD_ALIGN_PARAGRAPH.CENTER
title.runs[0].font.color.rgb = RGBColor(0x1F, 0x49, 0x7D)
title.runs[0].font.size = Pt(20)

subtitle = doc.add_paragraph('Презентация для руководства и ИТ-специалистов')
subtitle.alignment = WD_ALIGN_PARAGRAPH.CENTER
subtitle.runs[0].font.size = Pt(12)
subtitle.runs[0].italic = True

separator()

# ─── ВСТУПЛЕНИЕ ──────────────────────────────────────────────────
heading1('Вступление')
para(
    '«Добрый день. Я хочу представить вам систему, которую мы разработали для автоматизации '
    'одного из самых трудоёмких процессов в технологической подготовке производства — '
    'нормирования операций и стандартизации маршрутов.»',
    italic=True
)
separator()

# ─── ЧАСТЬ 1 ─────────────────────────────────────────────────────
heading1('Часть 1. Что такое нормирование и зачем оно нужно')
para('Нормирование — это определение времени, необходимого для выполнения каждой производственной операции.', bold=True)
separator()
para('Два ключевых показателя:', bold=True)
bullet(' время на изготовление одной детали на конкретном станке (в минутах)', bold_prefix='t_шт (штучное время) — ')
bullet(' время на настройку оборудования перед партией (в минутах)', bold_prefix='t_пз (подготовительно-заключительное время) — ')
separator()
para('Для чего это нужно производству:', bold=True)
bullet('Расчёт себестоимости изделия — без норм времени невозможно посчитать, сколько стоит деталь')
bullet('Планирование загрузки оборудования — диспетчер должен знать, сколько времени займёт операция')
bullet('Расчёт зарплаты рабочих — сдельная оплата невозможна без норм')
bullet('Сроки выполнения заказов — нет нормы = нет точной даты готовности')
separator()
para('Проблема: Нормировщик вручную изучает чертёж, маршрутную карту, выбирает режимы резания, '
     'проверяет наличие оборудования в цеху — это занимает от 20 минут до нескольких часов на одну деталь.', italic=True)
separator()

# ─── ЧАСТЬ 2 ─────────────────────────────────────────────────────
heading1('Часть 2. Что такое стандартизация маршрутов')
para('Маршрут детали — это последовательность операций: например, Токарная → Фрезерная → Шлифовальная → Контрольная.')
separator()
para('Проблема, которую мы решали:', bold=True)
para('В базе 1С одна и та же операция называлась по-разному: «Токарная», «Токарная обработка», '
     '«Обработка на токарном станке», «ТОК» — система воспринимала их как разные операции. Из-за этого:')
bullet('Аналитика была невозможна — нельзя было посчитать, сколько токарных операций в производстве')
bullet('Планирование работало неточно')
bullet('Типовые маршруты нельзя было выделить')
separator()
para('Что было сделано:', bold=True)
bullet('Утверждён реестр из 49 стандартных операций (Токарная, Фрезерная, Сварочная и т.д.)')
bullet('Разработана таблица соответствий: 173 названия из 1С → 49 стандартных')
bullet('Нормализованы 4 235 уникальных маршрутов производства')
separator()
para('Результат стандартизации:', bold=True)
para('Выделены топ-233 маршрута, которые покрывают 80% всего производства — это классический принцип Парето. '
     'Зная эти маршруты, можно нормировать их заранее, а не каждый раз вручную.')
separator()

# ─── ЧАСТЬ 3 ─────────────────────────────────────────────────────
heading1('Часть 3. Как работает программа')
para('Шаг 1 — Технолог загружает документы:', bold=True)
bullet('Маршрутная карта (PDF) — обязательно')
bullet('Чертёж детали (PDF) — по возможности')
separator()
para('Шаг 2 — Нажимает «Анализировать». Система автоматически:', bold=True)
bullet('Читает PDF-документы')
bullet('Определяет список операций и их последовательность')
bullet('Выбирает подходящее оборудование из базы завода (302 единицы)')
bullet('Учитывает, в каком цеху находится деталь, и ищет оборудование сначала там')
bullet('Рассчитывает t_шт и t_пз с обоснованием')
separator()
para('Шаг 3 — Технолог проверяет и корректирует:', bold=True)
para('Все поля редактируемые. Программа предлагает — технолог принимает решение.')
separator()
para('Шаг 4 — Сохранение в базу данных:', bold=True)
para('Подтверждённые нормы сохраняются с датой, деталью, операцией, оборудованием и обоснованием. '
     'История всегда доступна.')
separator()

# ─── ЧАСТЬ 4 ─────────────────────────────────────────────────────
heading1('Часть 4. Технологическая основа (для ИТ-специалистов)')
bullet('Бэкенд: Python / Flask — лёгкий, без избыточных зависимостей')
bullet('Фронтенд: Vanilla JS — без фреймворков, работает в любом браузере')
bullet('База данных: SQLite — не требует администрирования сервера')
bullet('ИИ-ядро: Claude (Anthropic) — языковая модель, читающая PDF и рассуждающая по инженерным задачам')
bullet('База оборудования: JSON-файл с 366 единицами оборудования завода, фильтрация по статусу')
separator()
para('Два режима работы:', bold=True)
bullet('Через Anthropic REST API — облачный вызов')
bullet('Через Claude Code CLI — локальный вызов без передачи данных во внешние системы')
separator()

# ─── ЧАСТЬ 5 ─────────────────────────────────────────────────────
heading1('Часть 5. Плюсы системы')
para('Для производства:', bold=True)
bullet('Скорость — норма рассчитывается за 30–60 секунд вместо 20–60 минут вручную')
bullet('Единообразие — одинаковые детали получают сопоставимые нормы, нет субъективности нормировщика')
bullet('Прозрачность — каждая норма имеет текстовое обоснование: почему выбран именно этот режим и это оборудование')
bullet('Привязка к реальному оборудованию — система знает, что есть на заводе, и не предложит несуществующий станок')
bullet('Накопление базы знаний — каждая подтверждённая норма остаётся в истории')
separator()
para('Для технолога:', bold=True)
bullet('Не заменяет специалиста, а снимает рутину')
bullet('Технолог проверяет и корректирует, а не считает с нуля')
bullet('Освобождает время для сложных нестандартных задач')
separator()
para('Для руководства:', bold=True)
bullet('Прослеживаемость: видно, кто и когда подтвердил норму')
bullet('Основа для анализа: можно строить отчёты по цехам, типам операций, оборудованию')
separator()

# ─── ЧАСТЬ 6 ─────────────────────────────────────────────────────
heading1('Часть 6. Ограничения и риски')
bullet('ИИ может ошибаться — особенно на нестандартных деталях или при нечётком чертеже. '
       'Именно поэтому технолог всегда проверяет результат — система помогает, но не решает за человека.')
bullet('Качество входных данных — если PDF маршрутной карты плохого качества или нечитаемый, точность снижается.')
bullet('Зависимость от актуальности базы оборудования — если станок сломался или появился новый, '
       'нужно обновлять базу вручную.')
bullet('Не заменяет нормировщика полностью — для уникальных операций или сложных допусков нужна экспертиза человека.')
bullet('Облачная модель — при использовании REST API данные (PDF) отправляются в Anthropic. '
       'Для конфиденциальных изделий это нужно учитывать. Альтернатива — локальный режим через CLI.')
separator()

# ─── ЗАКЛЮЧЕНИЕ ──────────────────────────────────────────────────
heading1('Заключение')
para(
    '«Эта система — не попытка заменить технолога или нормировщика. Это инструмент, который берёт на себя '
    'рутинную часть работы: прочитать документы, найти оборудование, прикинуть режимы. Специалист получает '
    'готовый черновик и тратит время на проверку и экспертизу, а не на поиск информации.\n\n'
    'В перспективе — накопленная база норм позволит строить типовые нормы для стандартных маршрутов, '
    'и большинство деталей будут нормироваться мгновенно, без обращения к ИИ вообще.\n\n'
    'Готов ответить на вопросы.»',
    italic=True
)
separator()

# ─── ВОПРОСЫ И ОТВЕТЫ ────────────────────────────────────────────
heading1('Возможные вопросы и ответы')

heading2('— Как система узнаёт правильное время?')
para('Языковая модель обучена на огромном массиве инженерных данных. Она учитывает материал, тип обработки, '
     'геометрию, квалитет допусков — и на основе этого предлагает норму. Это не поиск по таблице, '
     'а рассуждение, как у опытного нормировщика.')

heading2('— Можно ли доверять таким нормам?')
para('Система всегда предлагает, технолог всегда подтверждает. В базу попадают только подтверждённые нормы. '
     'Ответственность остаётся за человеком.')

heading2('— Сколько стоит работа ИИ?')
para('Один анализ (маршрутная карта + чертёж) стоит в районе 1–3 рублей по тарифам Anthropic. '
     'При экономии 30+ минут технолога — окупается моментально.')

heading2('— Можно ли подключить к 1С?')
para('Да, это следующий шаг. Сейчас система работает автономно, но API-интеграция с 1С технически несложна.')

# ─── СОХРАНЕНИЕ ──────────────────────────────────────────────────
output_path = '/Users/liudmila/Проект "Нормирование"/Презентация_система_нормирования.docx'
doc.save(output_path)
print(f"Файл сохранён: {output_path}")
